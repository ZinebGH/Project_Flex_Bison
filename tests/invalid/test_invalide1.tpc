
struct eddd{
	int a;
	char b;
;

struct  un, deux;



int fct_1(void){
	int a, b;
	char y;
	a = 3;
	y = 'e;
	
	/Erreur ce n'est pas un commentaire
	if(a >= 1 || a >= 1 + 4)
		a = 6;
	else
		a = 15);
	
	while(a < b && a > 34)
		// Ceci est un commentaire d'une ligne
		b = 9;
	tessst_03(aaa, aa);
	return a;
	return ab;
	return 0;
	{return 0;}
	abc
	print(6);
	/*Ceci//i est un commen/*t*a*ire 
	 * avec saut de ligne*/
	
}
